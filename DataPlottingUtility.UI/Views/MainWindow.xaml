<Window x:Class="DataPlottingUtility.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DataPlottingUtility.UI"
        xmlns:converters="clr-namespace:DataPlottingUtility.UI.Converters"
        xmlns:wpf="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="Data Plotting Utility" 
        Width="1200" Height="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
    </Window.Resources>

    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open..." Command="{Binding LoadFileCommand}" 
                          InputGestureText="Ctrl+O"/>
                <Separator/>
                <MenuItem Header="Export Plot..." Command="{Binding ExportPlotCommand}" 
                          InputGestureText="Ctrl+E"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About"/>
            </MenuItem>
        </Menu>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="Loading..." 
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Channel List -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="0,0,1,0">
                <DockPanel>
                    <!-- X-Axis Selection at the top -->
                    <Border DockPanel.Dock="Top" BorderBrush="LightGray" BorderThickness="0,0,0,1" Margin="5">
                        <StackPanel Margin="5">
                            <TextBlock Text="X-Axis Channel:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding XAxisChannels}" 
                                      SelectedItem="{Binding PlotConfig.XAxisChannelName, UpdateSourceTrigger=PropertyChanged}"
                                      Width="200"
                                      HorizontalAlignment="Left"
                                      SelectionChanged="XAxisChannelChanged">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <!-- Channels List -->
                    <TextBlock DockPanel.Dock="Top" Text="Channels" 
                               FontSize="16" FontWeight="Bold" 
                               Margin="10"/>

                    <ListBox ItemsSource="{Binding Channels}" 
                             Margin="5"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsSelected}" 
                                              VerticalAlignment="Center"
                                              Margin="0,0,10,0"/>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" 
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Unit}" 
                                                   FontSize="10" 
                                                   Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Right: Plot Area -->
            <Grid Grid.Column="1">
                <!-- Placeholder when no data -->
                <TextBlock Text="Load a CSV file to begin"
                           FontSize="24"
                           Foreground="Gray"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding IsDataLoaded, 
                                       Converter={StaticResource InverseBoolToVisConverter}}"/>

                <!-- Plot area (when data loaded) -->
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              Visibility="{Binding IsDataLoaded, 
                                          Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding PlotControls}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="LightGray" BorderThickness="1" Margin="5">
                                        <wpf:WpfPlot DataContext="{Binding}" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </DockPanel>
</Window>